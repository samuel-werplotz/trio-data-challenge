# 13.5 — SYNC-WORKER E BACKUP  [carga-real]

> **Etapa de reconciliação.** Registra na esteira trabalho que foi executado
> fora dela (commits `4051e42`, `bb29020`, `f8a0e2e`, `17a90dc`), sob a
> nomenclatura E0/E1/E2/E4 do `AUDITORIA-E-REPLANEJAMENTO.md` § 4.6. Nasce
> CONCLUÍDA: o objetivo é fechar o descompasso entre o roadmap e o repositório,
> não refazer o trabalho.

## ORIGEM
`AUDITORIA-E-REPLANEJAMENTO.md` § 4.1 (decisão de trocar CDC por micro-batch), § 4.3 (contrato do sync-worker), § 4.6 (roadmap E0–E7), § 5 (política de backup); `../vault-estudo/06-Especificacao/S05-Pipeline-CDC.md` (contrato do consumidor, reaproveitado); `../vault-estudo/06-Especificacao/S07-Legado-e-Backup.md` § Parte 2 e § Parte 3

## IMPEDITIVOS
- [ ] ficha `scripts/ambiente/DOCKER-LOCAL.md` tem campo `<PREENCHER>` → preencher e confirmar seed concluído

## ESTADO HERDADO
Verificado ao fechar a etapa 13 (descartada):
- Consumidor CDC completo em `desafio-2/pipeline/consumer/` — funcional, mas sobre um transporte que não funciona. `sink.py`/`metrics.py`/`config.py` reaproveitáveis.
- Slot `trio_cdc_slot` ativo e parado, retendo WAL e crescendo.
- 2 linhas `DEMO-CDC` em `transactions` quebrando 4 testes da suíte.
- `--profile core up` abortando (`grafana` depende de `prometheus`, que só existe no profile `full`).
- 3 serviços com `build:` para diretórios inexistentes.
- `archive_command` apontando para pgBackRest ausente da imagem.

## ESCOPO
Faz: estabilização do ambiente (P0 da auditoria), verificação de premissas antes de código, `sync-worker` por watermark de `updated_at` substituindo o CDC, e backup/recovery dos 3 bancos.
Não faz: não usa Debezium/Redpanda no caminho crítico — preservados no profile `cdc-experimento` como artefato da decisão. Não provisiona AWS.

## PASSOS
1. **E0 — Estabilização.** Backup dos 3 volumes + drill de restauração executado. Remover slot/conector/publication. `archive_mode=off` até o binário existir. Corrigir `grafana`→`prometheus`. Mover serviços sem Dockerfile para o profile `pendente`. Remover as 2 linhas `DEMO-CDC`.
2. **E1 — Verificação de premissas** antes de qualquer código: trigger de `updated_at`, índice em `updated_at`, `pgbackrest`/`clickhouse-backup` nas imagens, `BACKUP` nativo do ClickHouse, `pg_dump`. Registrar em `PREMISSAS-VERIFICADAS.md`.
3. **E2 — sync-worker.** `sync_state` (watermark), laço lê→converte→escreve→**só então** avança o watermark, métricas com prefixo `sync_`.
4. **E4 — Backup dos três bancos** (PDF § 5.2 A.1). pgBackRest nas 2 stanzas (TimescaleDB e PostgreSQL **legado**) → MinIO via S3/HTTPS; `BACKUP` nativo do ClickHouse. Para cada um dos 3 bancos: tipo, frequência, retenção e **destino AWS de produção** (bucket S3, `lifecycle` policy, réplica `cross-region`) documentados em `desafio-3/backup/README.md`. `restore-drill.sh` com as **3 contagens** do PDF § 5.2 A.2 — antes da perda, depois da perda, após o recovery. Religar `archive_mode` agora que o binário existe.
5. Acrescentar os blocos correspondentes em `scripts/tests/run_all.sh`.

## CRITÉRIOS DE ACEITE
- [x] `docker compose --profile core up -d` sobe limpo (critério nº 1 do PDF § 6.1)
- [x] `pg_replication_slots` vazio; `pg_wal` estabilizado (17,7 GB → 1,0 GB)
- [x] `transactions` com exatos 10.000.000; os 4 testes que as linhas `DEMO-CDC` quebravam voltam a passar
- [x] Premissas verificadas com comando e saída real antes do código — 3 de 6 refutadas
- [x] UPDATE no TimescaleDB aparece no ClickHouse em <30s (medido: ~4s)
- [x] Idempotência: watermark recuado 1h, `count() FINAL` idêntico
- [x] Resiliência: destino derrubado no meio do ciclo → watermark não avança, linha recuperada sozinha
- [x] `pgbackrest check` OK nas 2 stanzas; `BACKUP` do ClickHouse funcional
- [x] `restore-drill.sh` imprime as 3 contagens e mede RTO/RPO com número
- [x] Restore em instância paralela (5499), nunca sobre o principal

## TESTES
| id | trilha | comando | esperado |
|---|---|---|---|
| E0.1 | compose | `docker compose --profile core up -d` | sobe sem erro |
| E0.2 | compose | `archive_mode=on` ⇒ `pgbackrest` existe na imagem | invariante |
| E2.x | carga-real | INSERT/UPDATE na origem + esperar ciclo | reflete no ClickHouse |
| E2.y | carga-real | watermark recuado + novo ciclo | `count() FINAL` estável |
| E4.x | compose | `pgbackrest --stanza=timescale check` / `--stanza=legado check` | sai 0 |
| E4.y | compose | `bash desafio-3/backup/restore-drill.sh` | 3 contagens + RTO/RPO |

> Ids no formato `E<n>.<x>` porque os blocos já existem assim no `run_all.sh`,
> escritos quando estas entregas rodaram fora da esteira. Renomear agora só
> criaria ruído sem ganho.

## ROLLBACK
```bash
docker compose stop sync-worker
git checkout -- desafio-2/pipeline/sync-worker/ desafio-3/backup/ init/timescaledb/06_sync_state.sql
```
> Backup e restore-drill não alteram o dataset principal.

## STATUS
Estado: CONCLUÍDA (retroativo — executada como E0/E1/E2/E4 antes desta reconciliação)

Premissas assumidas:
- Micro-batch por watermark substitui o CDC. Não é plano B: o PDF § 4.2 A.1 lista a abordagem em pé de igualdade com CDC e cobra a justificativa escrita, não a ferramenta.
- ClickHouse usa `BACKUP` nativo em vez de `clickhouse-backup` (ausente da imagem, refutado no E1).
- pgBackRest roda dentro do container do banco via imagem derivada — instalar por `docker exec` não sobrevive ao primeiro `compose down`.
- MinIO em HTTPS com certificado auto-assinado: `repo1-type=s3` sempre fala TLS, sem opção de HTTP puro.
- Janela de `created_at` de 7 dias no predicado duplo do sync-worker.
- Serviços sem Dockerfile movidos para o profile `pendente`; CDC preservado em `cdc-experimento`.

Desvios do plano:
1. **Executada fora da esteira**, sob nomenclatura E0–E4, sem arquivo de etapa e sem `ESTADO HERDADO`. Esta etapa é a reconciliação. Registrado em `99-validacao-final.md`.
2. **Teste E0.2 inverteu de sinal** ao longo da execução: era "`archive_mode` deve estar off" (enquanto o binário não existia) e virou a invariante que de fato importa — se estiver `on`, o `pgbackrest` tem de existir na imagem.
3. **3 de 6 premissas refutadas** no E1 (índice em `updated_at` inexistente; `pgbackrest` e `clickhouse-backup` ausentes das imagens). Todas virariam bug com sintoma distante da causa.

## FECHAMENTO
- [x] Critérios atendidos
- [x] Testes no run_all.sh (blocos `E0.`, `E1.`, `E2.`, `E4.`)
- [x] run_all.sh sem FAIL — 118 pass, 0 fail, 3 skip
- [x] ESTADO HERDADO da próxima preenchido (14)
- [x] Bloco no LOG-EXECUCAO.md
- [x] Desvio? → registrado em `99-validacao-final.md`
- [x] Commit checkpoint (`4051e42`, `bb29020`, `f8a0e2e`, `17a90dc`)
- [x] Mover pra concluidas/. Marcar [x] no CLAUDE.md
